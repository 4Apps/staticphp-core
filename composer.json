{
    "name": "4apps/staticphp-core",
    "description": "A lightweight PHP framework",
    "license": "MIT",
    "type": "library",
    "homepage": "https://4apps.github.io/staticphp-core/",
    "authors": [
        {
            "name": "Gints Murans",
            "email": "gm@gm.lv"
        }
    ],
    "support": {
        "docs": "https://4apps.github.io/staticphp-core/",
        "issues": "https://github.com/4Apps/staticphp-core/issues",
        "source": "https://github.com/4Apps/staticphp-core"
    },
    "require": {
        "php": ">=8.4 <9",
        "ext-intl": "*",
        "ext-mbstring": "*",
        "ext-pdo": "*"
    },
    "require-dev": {
        "phpoffice/phpspreadsheet": "^3.0 || ^4.0",
        "phpstan/phpstan": "^2.2",
        "phpstan/phpstan-phpunit": "^2.0",
        "phpunit/phpunit": "^13.0",
        "squizlabs/php_codesniffer": "^4.0",
        "twig/twig": "^3.0"
    },
    "suggest": {
        "twig/twig": "Template engine for the view layer. Leave it out of an api-only application - composer's files autoload is eager, so twig and the symfony polyfills it pulls in load eight files on every request whether or not a template is ever rendered.",
        "phpoffice/phpspreadsheet": "Required by Presentation\\Models\\Tables\\Output\\Excel. Nothing else in the package touches it, so a table that only renders html does not need it.",
        "mongodb/mongodb": "Required by Utils\\Models\\Sessions\\SessionsMongoDb.",
        "ext-redis": "Required by Utils\\Models\\Cache\\CacheRedis, Utils\\Models\\Sessions\\SessionsRedis and the redis queue driver. The queue defaults to the database driver, which needs nothing extra.",
        "ext-memcached": "Required by Utils\\Models\\Cache\\CacheMemcached and Utils\\Models\\Sessions\\SessionsMemcached.",
        "ext-apcu": "Required by Utils\\Models\\Cache\\CacheApcu and Utils\\Models\\Sessions\\SessionsApcu.",
        "ext-iconv": "Required by Fv::translit() and Fv::setFriendly(); nothing else uses it.",
        "ext-pcntl": "Lets Utils\\Models\\Queue\\Worker finish the job in hand on SIGTERM instead of being killed mid-job, and applies the per-job timeout. Without it the queue still runs, but a restart loses the running job until its reservation expires.",
        "ext-sodium": "Required by Utils\\Models\\Crypto\\Crypto. Bundled and enabled by default in most php builds; an application that never encrypts a column does not need it.",
        "ext-pdo_pgsql": "Postgres connections, Utils\\Models\\Sessions\\SessionsPgsql, and the postgres migration driver.",
        "ext-pdo_mysql": "MySQL/MariaDB connections and the mysql migration driver.",
        "ext-pdo_sqlite": "SQLite connections and the sqlite migration driver."
    },
    "autoload": {
        "psr-4": {
            "StaticPHP\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "StaticPHP\\Tests\\": "tests/"
        }
    },
    "config": {
        "sort-packages": true
    },
    "scripts": {
        "test": "@php vendor/bin/phpunit",
        "lint": "@php vendor/bin/phpcs --standard=phpcs.xml src tests",
        "stan": "@php vendor/bin/phpstan analyse --no-progress --memory-limit=1G",
        "stan-baseline": "@php vendor/bin/phpstan analyse --no-progress --memory-limit=1G --generate-baseline"
    }
}
